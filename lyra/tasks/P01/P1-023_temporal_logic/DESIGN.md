# Design — P1-023 Temporal Logic

The executable temporal surface is built into the compiler pipeline through builtin call operators instead of new tokens, keeping the implementation coherent with the current parser while still providing first-class type-kernel and analysis support.
